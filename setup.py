from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ML project to predict the demand of uber cabs',
    author='Aniket',
    license='',
)
